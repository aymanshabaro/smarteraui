# Changesets

This folder holds [changesets](https://github.com/changesets/changesets): small markdown files
describing user-facing changes. Add one with `pnpm changeset` in any pull request that changes
`@properui/ui` or the `properui` CLI. The release workflow consumes them to version and publish.
