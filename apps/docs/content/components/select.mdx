---
title: Select components
section: base
description: Free and open-source React select components built for modern applications and websites. These select components are built using React Aria and styled with Tailwind CSS.
metaTitle: Free React select components | Proper UI
metaDescription: Free and open-source React select components built for modern applications and websites. These select components are built using React Aria and styled with Tailwind CSS.
install: select
source: packages/ui/src/components/base/select
demoFile: select
---

<Preview id="select-example" title="Select example" height={348} demo="select:SelectExample" />

## Installation

<Install slug="select" />

## Select examples

<Preview id="default" title="Default" height={348} demo="select:Default" />

<Preview id="disabled" title="Disabled" height={348} demo="select:Disabled" />

<Preview id="sizes" title="Sizes" height={594} demo="select:Sizes" />

<Preview id="icon-leading" title="Icon leading" height={348} demo="select:IconLeading" />

<Preview id="avatar-leading" title="Avatar leading" height={348} demo="select:AvatarLeading" />

<Preview id="dot-leading" title="Dot leading" height={348} demo="select:DotLeading" />

<Preview id="search" title="Search" height={348} demo="select:Search" />

<Preview id="tags" title="Tags" height={348} demo="select:Tags" />

## FAQs

<FAQs
    items={[
        {
            q: "What sizes are available for the select?",
            a: 'The select supports three sizes: "sm" , "md" , and "lg" . The default size is "sm" .',
        },
        {
            q: "How do I disable the select?",
            a: "You can disable the select by setting the isDisabled prop to true .",
        },
        {
            q: "Can I add a search to the select?",
            a: "Yes. Use the Select.ComboBox compound component instead of Select to enable search/filtering of options.",
        },
        {
            q: "How do I add icons or avatars to select items?",
            a: "Pass icon for a leading icon or avatarUrl for a leading avatar on Select.Item . You can also pass icon on the Select component itself to show an icon in the trigger.",
        },
        {
            q: "Is the select accessible?",
            a: "Yes. The select is built on React Aria Components, which provides full keyboard navigation, screen reader support, and ARIA attributes out of the box.",
        },
        {
            q: "What is the trigger's accessible name when a label is set?",
            a: "Exactly the label text — not the label plus the current value. If you need the current value announced too, add it to your own aria-label or read it from the visible trigger content.",
        },
        {
            q: "Does Select.Item's textValue include supportingText?",
            a: 'Yes, by default the accessible textValue used for typeahead is `label + " " + supportingText` when supportingText is set. Pass your own `textValue` on Select.Item to override this.',
        },
        {
            q: "How do I open the select's popover in a jsdom test?",
            a: "userEvent.click alone toggles it open then shut in the same interaction. Use fireEvent.click(trigger), or focus the trigger and press ArrowDown. See the Testing page for the full recipe.",
        },
        {
            q: "How do I control when ComboBox's menu opens, or use my own filtering?",
            a: 'Pass menuTrigger ("focus" by default, or "input" / "manual") to control when the popover opens. ComboBox still applies React Aria\'s own `contains` filter on top of whatever items you pass in — pass defaultFilter={() => true} to disable it and rely entirely on app-side filtering. allowsEmptyCollection keeps the popover open (showing an empty state) instead of closing it when nothing matches.',
        },
        {
            q: "Is there a plain native <select> option?",
            a: "Yes, NativeSelect renders a real <select> element (styled to match) instead of a listbox popover. It honours a caller-supplied id (falling back to a generated one), accepts a readonly options array, and renders an optional placeholder as a disabled first option.",
        },
    ]}
/>
