---
title: MCP integration
section: integrations
description: A Model Context Protocol server for Smartera UI is planned but not yet built. This page describes the intent for anyone who wants to build it.
metaTitle: MCP (Model Context Protocol) integration | Smartera UI
metaDescription: Smartera UI does not ship an MCP server today. This page describes the planned design and links to the GitHub repo for contributors.
---

> **Planned, not built.** There is no `smarteraui-mcp` package and nothing to install today — `packages/` in the [Smartera UI repo](https://github.com/aymanshabaro/smarteraui) holds only `cli`, `registry`, and `ui`. This page describes the intent for an MCP server, for anyone who'd like to build it.

## The idea

The `smarteraui` CLI already exposes the whole registry through commands — `list`, `search`, `add`, `diff` (see the [CLI docs](/docs/cli)). An [MCP](https://modelcontextprotocol.io) server would wrap that same registry so an MCP-aware assistant could search for and install components by description, without you leaving the chat.

### What it would let you do

- Search the registry by description instead of remembering an exact component name — the MCP equivalent of `smarteraui search`.
- Look up a component's props, examples, and dependencies before deciding to install it.
- Have the assistant run the equivalent of `smarteraui add`, including its dependency chain.
- Ask for a full page example and get every component it composes in one pass, the way `smarteraui add example <name>` does from the terminal.

### How it would work

The intent is for the server to wrap the same registry JSON the CLI already reads (`<registry>/<name>.json`) rather than generating or guessing anything, and for its install operations to follow the same rules `add` already implements: resolving `registryDependencies`, rewriting `@/` imports to the consuming project's configured alias, and reporting which files changed.

## Building it

The pieces this would sit on top of already exist and are documented, if you want to take it on:

- The registry format and build step — `packages/registry` (see `packages/registry/src/build.ts`) and the `pnpm registry:build` script.
- The install logic to mirror — `packages/cli/src/commands/add.ts`, `list.ts`, `search.ts`, and `diff.ts`.
- The CLI's own command surface — documented on the [CLI page](/docs/cli) and in `docs/cli.md`.

Open an issue or a pull request on the [GitHub repo](https://github.com/aymanshabaro/smarteraui) if you'd like to build this — there's no `packages/mcp` yet, so there's no existing implementation to work around.

## FAQs

<FAQs
    items={[
        {
            question: "Is there an MCP server for Smartera UI today?",
            answer: "No. This page describes a planned design, not a shipped package — there's nothing named smarteraui-mcp to install.",
        },
        {
            question: "Can I still use an AI coding assistant to add components?",
            answer: "Yes, today, through the CLI directly: ask your assistant to run npx smarteraui@latest add <component> (or search, list, diff) in its shell tool. That works right now and needs no MCP server.",
        },
        {
            question: "Where do I go to help build this?",
            answer: "The GitHub repo — github.com/aymanshabaro/smarteraui. The registry format and the CLI's add/list/search/diff commands are the pieces an MCP server would wrap.",
        },
    ]}
/>

The MCP server is tracked with the project's other planned work on the
[roadmap](https://github.com/aymanshabaro/smarteraui/blob/main/ROADMAP.md).
