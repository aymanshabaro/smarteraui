node_modules
.next
dist
storybook-static
.turbo
.shots
*.tsbuildinfo
.DS_Store
.env*
!.env.example

# Internal build specification and captured reference material.
# Not redistributable — see LICENSES/ and NOTICE. Kept locally, never published.
docs/spec/


# Generated screenshots (not thumbnails — apps/docs/public/thumbs/ is committed,
# generated by `pnpm shots:thumbs`, and used as the gallery's images)

# Diff images from `pnpm visual:check` — the baseline itself (tests/visual/baseline/) is committed.
tests/visual/.diff/

# OpenNext Cloudflare build output (apps/docs)
.open-next
.wrangler
cloudflare-env.d.ts
.next-verify/
.build-verify/

# Local editor / agent config
.claude/
