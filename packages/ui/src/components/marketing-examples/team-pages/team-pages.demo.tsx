import { TeamPage01 } from "./team-page-01";

/** The docs page renders the same variant the reference preview does: `team-page-01`. */
export const TeamPageExample = () => (
    <div className="bg-primary w-full">
        <TeamPage01 />
    </div>
);
