import { Dashboard01 } from "./dashboard-01";

/** The hero example the reference docs page shows: the analytics dashboard under a dual-tier header. */
export const DashboardWithHeaderNavigationExample = () => (
    <div className="bg-primary w-full">
        <Dashboard01 />
    </div>
);
